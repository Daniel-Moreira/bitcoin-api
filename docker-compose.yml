version: '3.3'
services:
  # api:
  #   build: ./api
  #   ports:
  #    - "3000:3000"
  #   depends_on:
  #    - db
  db:
    build: ./mysql
    ports:
      - "33060:3306"
    networks:
      - local_network
    environment:
      MYSQL_DATABASE: db
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
networks:
  local_network:
